import {
  Meta,
  Canvas,
  Story,
  ArgsTable,
  Source
} from '@storybook/addon-docs/blocks';
import { Slider } from './Slider';

# Slider

<Meta
  title="Slider/Slider"
  component={Slider}
/>

The slider component will be used in the app to select a mood.

In essence, this component receives an array of steps to be selected, containing a color, icon, percentage, optional name and if the item should be default

## Default Usage

Here's an example with just a basic range supplied to it, and no back talking to its parent to act as a playground.

import {
  argTypes as defaultArgTypes,
  args as defaultArgs,
  parameters as defaultParameters,
  Template as DefaultTemplate,
} from './stories/default';

<Canvas>
  <Story
    name="Default Usage"
    argTypes={defaultArgTypes}
    args={defaultArgs}
    parameters={defaultParameters}
  >
    {DefaultTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default Usage" />


## Emotion Example

And this is a more complex example, more real-world, with the component reporting on changes to its parent component, and using mood icons

import {
  argTypes as emotionArgTypes,
  args as emotionArgs,
  parameters as emotionParameters,
  Template as EmotionTemplate,
} from './stories/emotion';

<Canvas>
  <Story
    name="Emotion Example"
    argTypes={emotionArgTypes}
    args={emotionArgs}
    parameters={emotionParameters}
  >
    {EmotionTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Emotion Example" />



## Real World Example

This is how it's going to look like with the theme colors integrated.

import {
  argTypes as realworldArgTypes,
  args as realworldArgs,
  parameters as realworldParameters,
  Template as RealworldTemplate,
} from './stories/realworld';

<Canvas>
  <Story
    name="Real World Example"
    argTypes={realworldArgTypes}
    args={realworldArgs}
    parameters={realworldParameters}
  >
    {RealworldTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Real World Example" />
